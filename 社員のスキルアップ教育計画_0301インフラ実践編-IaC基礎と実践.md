[[ メニューに戻る ]](社員のスキルアップ教育計画_00menu.md)

# IaC基礎と実践<br>( Infrastructure as Code、CloudFormation、YAML、Deploy )

## 1. IaC( Infrastructure as Code )とは？

IaC( Infrastructure as Code )とは、  
サーバー・ネットワーク・権限設定などのインフラ構成を、**コードとして管理する考え方**です。

従来は管理画面から手作業で構築していましたが、IaCでは設定内容をファイル化し、再現可能な形で管理します。

- **目的**：構築作業の標準化、自動化、再現性向上
- **効果**：設定ミス削減、レビュー可能、変更履歴管理
- **代表例**：CloudFormation、Terraform

```plaintext
[従来]
　管理画面で手作業設定

[IaC]
　コードを書く → 実行 → 同じ環境を再現
```

👉 **インフラを人の記憶ではなく、コードで管理するのがIaCです。**

## 2. AWS標準IaC - CloudFormation

CloudFormation は AWS が提供する IaC サービスです。
YAML または JSON でAWS構成を定義し、その内容をAWSへ反映できます。

* AWS公式サービスのため相性が良い
* 多くのAWSサービスに対応
* 変更差分を安全に適用可能
* Stack単位で管理できる

```plaintext
YAMLファイル
   ↓
CloudFormation
   ↓
VPC / S3 / Lambda / IAM など作成
```

## 3. YAMLファイルの基本構造
CloudFormationでは YAML 形式でインフラ全体を定義します。  
1つのテンプレートの中に、複数のAWSリソースをまとめて記述できます。

```plaintext
Root
├─ AWSTemplateFormatVersion
├─ Parameters
├─ Resources( 複数リソース定義 )
│   ├─ 論理ID 1( Type: AWSリソース1 )
│   ├─ 　：
│   └─ 論理ID N( Type: AWSリソースN )
└─ Outputs
```

以下は主要要素を含んだ最小例です。

```yaml
AWSTemplateFormatVersion: '2010-09-09'

Parameters:
  env:
    Type: String
    Default: dev

Resources:

  AppLogBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub app-log-${env}
      Tags:
        - Key: Env
          Value: !Ref env

  LambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: LambdaRole
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole

  MyLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: MyFunction
      Runtime: python3.11
      Role: !GetAtt LambdaExecutionRole.Arn

Outputs:
  BucketName:
    Value: !Ref AppLogBucket
```
👉 CloudFormationは「単一サービスの定義」ではなく「複数リソースの集合」です。

### 📌リソース対応表
| # | 論理ID | Type | 役割 | 物理ID |
|---|---|---|---|---|
| 1 | AppLogBucket | S3 | データ保存 | app-log-dev |
| 2 | LambdaExecutionRole | IAM Role | 権限管理 | LambdaRole |
| 3 | MyLambda | Lambda | 処理実行 | MyFunction( 自動生成 or 指定可能 ) |

- 論理ID( Logical ID ): テンプレート内で管理する名前
- 物理ID( Physical ID ): AWS上で実際に作成されるリソース名

### ミニコラム　!Ref と !Sub とは？
CloudFormation では、他の値を参照したり、文字列に値を埋め込む専用記法があります。

| 記法 | 用途 | 例 | 結果(env=dev) |
|---|---|---|---|
| !Ref | 値を参照する | !Ref env | dev |
| !Sub | 文字列内に値を埋め込む | !Sub app-log-${env} | app-log-dev |

👉 `${env}` だけでは置換されず、`!Sub` が必要です。

## 4. Parameters( 外部入力値 )
Parameters は、環境ごとの差分を切り替えるための入力値です。
```yaml
Parameters:
  env:
    Type: String
    Default: dev
```
- Type : 値の種類(文字列、数値など)
- Default : 未指定時に使われる初期値

実行時に上書きもできます。
```bash
aws cloudformation deploy \
--parameter-overrides env=stg
```

利用例：
- dev / stg / prod 切替
- バケット名変更
- インスタンスサイズ変更
- 通知メールアドレス変更

👉 同じテンプレートでも、Parameters を変えるだけで別環境へ使い回せます。

## 5. Resources( リソース定義 )
Resources は「何を作るか」と「どう設定するか」を定義する最重要項目です。  
CloudFormation では、`Type` に作成するAWSリソース、`Properties` に詳細設定を記述します。  
作成されるリソースは単体で動作するのではなく、IAM Roleなどと組み合わせて利用されます。

例：S3バケットを作成する場合
```yaml
Resources:
  AppLogBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub app-log-${env}
      Tags:
        - Key: Env
          Value: !Ref env
```
このS3バケットは、IAM Roleによってアクセス権限が付与されることで はじめて、利用可能になります。

| 項目 | 内容 |
|---|---|
| AppLogBucket | 論理ID |
| Type | 作成する種類 |
| Properties | 詳細設定 |
| BucketName | S3バケット名 |
| Tags | 管理ラベル |

### この資料で扱う主な Type 一覧
| 分類 | Type |
|---|---|
| S3 | AWS::S3::Bucket |
| Lambda | AWS::Lambda::Function |
| IAM Role | AWS::IAM::Role |
| DynamoDB | AWS::DynamoDB::Table |
| VPC | AWS::EC2::VPC |
| Subnet | AWS::EC2::Subnet |
| Security Group | AWS::EC2::SecurityGroup |
| ECS Cluster | AWS::ECS::Cluster |
| ECS Service | AWS::ECS::Service |

👉 Resourcesは CloudFormation の中心であり、IAM や Outputs と連携して動作します。

## 6. Outputs( 出力値 )
Outputsは、作成したAWSリソースの情報を スタックの外部から参照できるようにする項目です。  
作成後にリソース名やURLなどを確認できます。

```yaml
Outputs:
  BucketName:
    Value: !Ref AppLogBucket
```
- 左側 `BucketName` は表示名
- `Value` に表示したいAWSリソース情報を指定します。

例：
- 作成したS3バケット名
- Lambda ARN
- API URL
- DB接続先Endpoint

👉 Parameters は入力値、Resources は作成内容、Outputs は作成結果です。

## 7. Tag( タグ設計 )
Tagは管理用ラベルとして、実務では非常に重要です。  
AWSリソースの識別やコスト管理に利用されます。
```
Tags:
  - Key: Project
    Value: ProjectA
  - Key: System
    Value: SalesApp
  - Key: Env
    Value: !Ref env
```
* `Key`：タグの分類名
* `Value`：設定値( 固定値またはパラメータ参照 )

### 📌 主な用途
| 用途 | 例 |
|---|---|
| コスト集計 | TeamA など |
| 環境識別 | dev / stag / prod |
| システム識別 | SalesApp など |

👉 タグ設計がない運用は、後で管理・集計が困難になります。

## 8. IAM Role( 権限設定 )
IAM Roleは、AWSサービスに対して「どのAWS操作を許可するか」を定義する仕組みです。

AWSではセキュリティ上、サービス同士が自由にアクセスできないため、  
IAM Roleを使って明示的に権限を付与します。

例えば Lambda は、単体では S3 にアクセスできません。  
そのため、IAM Roleを通じて「S3にアクセスしてよい」という許可を与えます。

### 📌 例：Lambda → S3アクセス
```plaintext id="iam-flow"
Lambda
  ↓ ( IAM Roleで許可 )
S3へアクセス可能になる
```

### 📌 よくある権限例
* Lambda → S3書き込み
* Lambda → CloudWatch Logs出力
* Firehose → S3保存

### 💡 重要ポイント
* AWSでは「ユーザに権限を付ける」のではなく
  **サービスにRoleを付けるのが基本**
* アクセスキー直書きは避ける
* Roleは最小権限で設計する

👉 IAM Roleは「操作の許可証」のようなものです

## 9. 環境分離( dev / stg / prod )
実務では、開発・検証・本番の環境を分けて運用します。  
※stg は staging(検証環境) の略です。

| 環境 | 用途 |
|---|---|
| dev  | 開発・検証 |
| stg  | 本番前確認・総合テスト |
| prod | 本番利用 |

IaCなら同じテンプレートで複数環境を作れます。

```plaintext
同じYAML
 ├─ dev環境
 ├─ stg環境
 └─ prod環境
```

## 10. 実行コマンド( 現場で重要 )

### 10-1 構文確認
```bash
aws cloudformation validate-template \
--template-body file://template.yaml
```

### 10-2 変更差分確認( 任意 )
本番環境などでは、反映前に変更内容を確認すると安全です。

```bash
aws cloudformation create-change-set \
--stack-name sample-stack \
--template-body file://template.yaml \
--change-set-name review-001
```

👉 何が作成・変更・削除されるか事前確認できます。

### 10-3 初回作成 / 更新
```bash
aws cloudformation deploy \
--template-file template.yaml \
--stack-name sample-stack \
--capabilities CAPABILITY_NAMED_IAM
```

### 10-4 削除
```bash
aws cloudformation delete-stack \
--stack-name sample-stack
```

## 10-5 学習時によくある削除失敗
CloudFormation で削除しても、内容物や依存関係により  
Stack が正常に削除できない場合があります。

* S3 バケット内にファイルが残っている
* IAM Role が他サービスから参照されている
* Network Interface などネットワーク関連が残っている

👉 削除失敗時は CloudFormation Events を確認し、原因リソースを先に解消してから再実行します。

## 11. 基本作業フロー
```plaintext
① 作りたい構成を整理
② YAML作成
③ validate
④ deploy
⑤ AWS環境確認
⑥ 修正して再deploy
⑦ 不要ならdelete
```

👉 実務では「いきなり書く」より、先に構成整理してから作る方が失敗しにくいです。

## 12. 学習時の注意( 課金と削除 )
CloudFormation自体は無料ですが、
作成されるAWSリソースには料金が発生する場合があります。

例：
- EC2：起動時間で課金
- RDS：起動中課金
- NAT Gateway：高額注意
- S3：低コスト

👉 学習後は delete-stack を忘れず実施します。

## 13. まとめ
* IaCはインフラをコードで管理する方法
* AWSではCloudFormationが代表的
* Resourcesで構成を定義する
* Parametersで環境差分を管理する
* Tag / IAM Role は実務で重要
* validate → deploy → update → delete が基本操作
* IaCはコード作成だけでなく、安全な変更管理と運用まで含む

👉 **IaCは「書くだけ」でなく「実行して運用する」までが本番です。**

## 14. 重要用語説明
| 用語 | 説明 |
|---|---|
| IaC( インフラコード管理 )<br>Infrastructure as Code | インフラ構成をコードで管理し、再現性と自動化を高める手法。 |
| CloudFormation( AWS構成管理 )<br>AWS CloudFormation | AWSリソースをテンプレートで作成・更新・削除できるサービス。 |
| YAML( 設定記述形式 )<br>YAML Ain't Markup Language | 人が読みやすい構成ファイル形式。 |
| Resources( リソース定義 )<br>Resources | 作成対象となるAWSサービスを記述する項目。 |
| Logical ID( 論理ID ) | CloudFormationテンプレート内で、各リソースを識別する名前。 |
| Parameters( 外部入力値 )<br>Parameters | 環境ごとに値を切り替えるための入力変数。 |
| Outputs( 出力値 )<br>Outputs | 作成後にIDやURLなどを表示する出力項目。 |
| Tag( 識別ラベル )<br>Tag | AWSリソースへ付与する管理用ラベル。 |
| IAM Role( 権限ロール )<br>Identity and Access Management Role | AWSサービスへ権限を委任する仕組み。 |
| Stack( 管理単位 )<br>Stack | CloudFormationでまとめて管理する構成単位。 |
| Deploy( 反映 )<br>Deploy | コードや設定をAWS環境へ適用すること。 |

[[ このページの先頭に戻る ]](#) [[ メニューに戻る ]](社員のスキルアップ教育計画_00menu.md)
