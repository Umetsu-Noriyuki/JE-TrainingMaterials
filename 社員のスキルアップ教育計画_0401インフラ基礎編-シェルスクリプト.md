[[ メニューに戻る ]](README.md)

### 1. そもそもシェルとは？

- **概要**
  - プログラムの一つで、ユーザがターミナルに入力したコマンドを解釈し、OSに指示を出す役割を担います。
  - コマンドラインインタープリタとも呼ばれています。
  - シェルという名前は「殻」を意味し、OSの核（カーネル）を包み込む層として機能することから、この名前が付けられました。

- **主な特徴**  
  - CLI（文字ベース）操作
  - OSとユーザの仲介役
  - 自動化（スクリプト）の仕組み
  - 現在一般的なシェルは「bash」。
  - 他にBourne Shellや、bash・ksh・tcshの便利な機能を取り込んだ「Zsh」などが存在します。

- **シェルスクリプトとは？**
  - シェルで実行できる複数のコマンドを一つのテキストファイルにまとめ、
  - 上から順番に連続して実行できるようにしたプログラム（スクリプトファイル）のことです。

- **どんな時に使うのか？（活用例）**
  - **定期的な作業の自動化**: 毎日決まった時間にバックアップを取得する、サーバーの状態を監視するなど。
  - **一括・大量処理**: 何百個もあるファイルの名前を特定のルールで一括変更する、不要な古いログファイルを一括削除するなど。
  - **環境構築・デプロイ**: 開発に必要なアプリケーションをコマンド一つでまとめてインストール・設定する。
  - **ファイル操作・ログ解析**: 膨大なシステムログ（テキストデータ）から、トラブルの原因となるエラーメッセージを瞬時に検索・抽出する。

---

### 2. シェルスクリプトの基本文法

シェルスクリプトを作成するには、拡張子「.sh」を持つテキストファイルを作成します。
このファイルの1行目には必ず「シバン（shebang）: `#!/bin/bash`」と呼ばれる特殊な行を記述します。
シバンはスクリプトを実行するインタプリタを指定する役割を持ちます。

シンプルにターミナル上に文字列を出力したい場合は「`echo`」を使用します。

**記述例："Hello, World!" を表示したい場合**
`sample_1.sh`
```bash
#!/bin/bash
echo "Hello, World!"
```

- **四則演算**
  シェルスクリプトで用意されている四則演算は以下の通りです。
  ※基本的な演算子は他プログラミング言語とほぼ同じですが、計算を行う際は `$(( 算式 ))` という構文で囲む必要があります。

  | 算術演算子 | 意味 | コマンドの記述例 | 実行結果 |
  | :--- | :--- | :--- | :--- |
  | `a + b` | aとbの和を計算する | `echo $(( 5 + 5 ))` | 10 |
  | `a - b` | aとbの差を計算する | `echo $(( 5 - 5 ))` | 0 |
  | `a * b` | aとbの積を計算する | `echo $(( 5 * 5 ))` | 25 |
  | `a / b` | aとbの商を計算する | `echo $(( 5 / 5 ))` | 1 |
  | `a % b` | aをbで割ったときの余りを計算する | `echo $(( 5 % 5 ))` | 0 |

- **条件式**
  シェルスクリプトの条件分岐（`if` 文）では、`[` と `]` の前後に**必ずスペースが必要**という厳格なルールがあります。

  #### ① 数値比較の演算子（整数用）
  数値を比較する場合は、記号ではなくアルファベットの演算子を使用します。

  | 演算子 | 意味 | 記述例（xが10の場合） |
  | :--- | :--- | :--- |
  | `-eq` | 等しい（Equal） | `[ $x -eq 10 ]` （真） |
  | `-ne` | 等しくない（Not Equal） | `[ $x -ne 10 ]` （偽） |
  | `-gt` | より大きい（Greater Than） | `[ $x -gt 5 ]` （真） |
  | `-ge` | 以上（Greater or Equal） | `[ $x -ge 10 ]` （真） |
  | `-lt` | 未満（Less Than） | `[ $x -lt 10 ]` （偽） |
  | `-le` | 以下（Less or Equal） | `[ $x -le 10 ]` （真） |

  #### ② 文字列の比較
  文字列を比較する場合は、記号（`=` や `!=`）を使用します。

  | 演算子 | 意味 | 記述例 |
  | :--- | :--- | :--- |
  | `=` | 文字列が一致する | `[ "$str" = "apple" ]` |
  | `!=` | 文字列が一致しない | `[ "$str" != "apple" ]` |
  | `-z` | 文字列の長さが0（空）である | `[ -z "$str" ]` |
  | `-n` | 文字列の長さが0より大きい（空でない） | `[ -n "$str" ]` |

  #### ③ ファイルチェック
  ファイルやディレクトリの状態を調べるための、実務で非常によく使う演算子です。

  | 演算子 | 意味 | 記述例 |
  | :--- | :--- | :--- |
  | `-f` | ファイルが存在し、かつ通常のファイルである | `[ -f "/path/to/log.txt" ]` |
  | `-d` | ディレクトリが存在する | `[ -d "/path/to/dir" ]` |
  | `-e` | ファイルまたはディレクトリが存在する | `[ -e "/path/to/target" ]` |
  | `-r` | ファイルが存在し、読み込み権限がある | `[ -r "file.txt" ]` |

  #### ④ 条件式の論理結合
  複数の条件を組み合わせる（かつ / または）方法です。`[ ... ]` 構文を使うと直感的に記述できます。
  - **かつ（AND）**：`-a`
  - **または（OR）**：`-o`

  **条件式を組み合わせた具体的なコード例**
  ```bash
  #!/bin/bash
  
  FILE_PATH="./error.log"
  
  # ファイルが存在し、かつ中身が空ではない（サイズが0より大きい）場合
  if [ -f "$FILE_PATH" -a -s "$FILE_PATH" ]; then
      echo "有効なログファイルが存在します。"
  else
      echo "ログファイルがないか、または空です。"
  fi
  ```

- **繰り返し処理（ループ）**
  特定の処理を何度も実行させたい場合は `for` 文や `while` 文を使用します。

  **記述例：1から5まで順番に出力する**
  ```bash
  #!/bin/bash
  for i in {1..5}
  do
      echo "カウント: $i"
  done
  ```

- **パイプ (`|`)**
  あるコマンドの「出力（結果）」を、別のコマンドの「入力」として引き渡す仕組みです。
  複数のコマンドを数珠つなぎにして高度な処理を行います。

  **記述例：大量のログの中から【Error】を見つける場合**
  ```bash
  cat app.log | grep "Error"
  ```
  （解説：`cat` で `app.log` の中身をすべて吐き出し、それをパイプ `|` で `grep` に渡して "Error" という文字列が含まれる行だけを抽出しています）

- **よく使うコマンド**
  - `cd`：ディレクトリの移動
  - `ls`：ファイルやディレクトリの一覧表示
  - `cat`：ファイル内容の表示
  - `grep`：特定の文字列が含まれる行の検索
  - `mkdir`：新しいディレクトリの作成
  - `rm`：ファイルやディレクトリの削除

---

### 3. 環境構築手順

シェルスクリプトを実行するための環境は、使用しているOSによって異なります。

- **Linux / macOS の場合**
  標準で `bash` や `Zsh` などのシェルが組み込まれているため、**特別な環境構築は不要**です。ターミナルを起動するだけで、すぐにスクリプトの作成・実行が可能です。

- **Windows の場合**
  Windowsの標準機能（Command Prompt / PowerShell）ではそのまま実行できないため、以下の手順で仮想環境を準備します。

  #### ① 事前準備
  本手順では、以下のインストーラおよびISOイメージを使用します。
  - **VirtualBox**: `VirtualBox-7.1.10-169112-Win.exe`
  - **Git Bash**: `Git-2.55.0.5-64-bit.exe`（Windows側での作業・連携用）
  - **Ubuntu ISO**: `ubuntu-ja-22.04-desktop-amd64.iso`

  #### ② 仮想環境の構築手順
  ##### ステップ1: ソフトウェアのインストール
  1. **VirtualBoxのインストール**
     `VirtualBox-7.1.10-169112-Win.exe` を実行し、画面の指示に従ってデフォルト設定でインストールします。
  2. **Git Bashのインストール**
     `Git-2.55.0.5-64-bit.exe` を実行し、デフォルト設定のままインストールを完了させます。

  ##### ステップ2: VirtualBoxでの仮想マシンの作成
  1. VirtualBoxを起動し、「新規」ボタンをクリックします。
  2. 以下の項目を設定します。（VirtualBoxでの設定のため、他ソフトでは表示が異なる場合あり）
     - **名前とオペレーティングシステム**
       - 名前: 任意の名前（例: `Ubuntu-22.04`）
       - ISOイメージ: `ubuntu-ja-22.04-desktop-amd64.iso` を選択
     - **自動インストール**
       - ユーザ名とパスワード、パスワードの再入力を設定（任意のものでOK）
     - **ハードウェア**（マシンスペックに応じて調整）
       - メモリ: `2048MB (2GB)` 以上（推奨: `4096MB`）
       - プロセッサー: `2 CPU` 以上
     - **ハードディスク**
       - 「仮想ハードディスクを作成する」を選択し、`25GB` 以上（推奨）に設定します。
  3. 「完了」をクリックします。

  ##### ステップ3: 仮想マシンの起動
  1. 作成した仮想マシンを選択し、「起動」をクリックします。
  2. インストーラが起動したら、画面の指示に従い「Ubuntuをインストール」を進めます。

  ##### ステップ4: 作成したユーザに管理者権限を付与（リカバリーモードから管理者権限を付与）
  1. Ubuntuのインストール後、起動した仮想マシンの電源を落とします。
  2. 再度仮想マシンを起動し、起動中の画面が表示されたら `Shift` キーを長押しします。
     - → リカバリーモードに移行する（GRUBメニュー（黒い選択画面））
  3. 「Advanced options for Ubuntu」を選択します。
  4. リストの中から、末尾に「(recovery mode)」と書かれた項目を選んで `Enter` を押します。
  5. しばらくすると「Recovery Menu」が表示されるので、下部にある `root (Drop to root shell prompt)` を選択して `Enter` を押します。
  6. 画面最下部に `Give root password for maintenance` と出たら、作成したユーザのパスワードを入力し `Enter` を押します。
  7. 書き込み権限を有効にするため、コマンド `mount -o remount,rw /` を入力します。
  8. 管理者権限を付与したいユーザ（自動インストールで設定した任意のユーザ）を指定して、コマンド `gpasswd -a 任意のユーザー名 sudo` を入力します。
     - （※ `Adding user ユーザー名 to group sudo` と表示されれば成功です）
     - *記述例*: `gpasswd -a test sudo`
  9. 設定後、コマンド `reboot` を入力して仮想マシンを再起動します。

  #### ③ 仮想マシンでのターミナル（端末）の起動
  1. 画面左下にあるメニュー（・が縦3、横3の合計9個表示しているボタン）を押下し、アプリケーション一覧を表示します。
  2. その中にあるターミナル（または端末）を選択し、起動します。

---

### 4. シェルスクリプトの実行方法

作成したシェルスクリプトを実行するには、通常以下の2つのステップを行います。

#### ステップ1：実行権限（パーミッション）を付与する
新しく作成したテキストファイルには、安全のため「実行する権利」が与えられていません。
`chmod`（チェンジモード）コマンドを使って権限を追加します。

```bash
chmod +x sample_1.sh
```
※ `+x` は「実行権限（e**x**ecute）を**追加**（`+`）する」という意味です。この操作はファイルごとに最初の1回だけ実行すればOKです。  
（数値で指定して権限を付与する記述例：`chmod 755 sample_1.sh`）

#### ステップ2：スクリプトを実行する
ファイル名の前に `./`（現在のディレクトリを意味するパス）を付けて実行します。

```bash
./sample_1.sh
```
※ Linuxの安全上のルールとして、現在自分がいるディレクトリ（カレントディレクトリ）にあるプログラムであっても、場所を明確に指定（`./`）しないと実行できない仕組みになっています。

**【Tips】権限を変更せずに実行する方法**
実行権限を付与していない状態でも、シェル（インタプリタ）を直接指定してファイルを読み込ませれば実行可能です。
```bash
bash sample_1.sh
```

---

### 5. 演習問題

ここまでの知識を使って、以下の演習問題に挑戦してみましょう。

#### ① 四則演算と文字列の出力
**【問題】**
変数 `x` に `15`、変数 `y` に `4` を代入し、この2つの変数の「積（掛け算）」と「`x` を `y` で割った余り（剰余）」を計算して、以下のような結果をターミナルに出力するシェルスクリプトを作成してください。

**期待される実行結果：**
```text
積の計算結果: 60
余りの計算結果: 3
```

#### ② 条件分岐（数値の比較とファイルチェック）
**【問題】**
カレントディレクトリに `test.txt` というファイルが存在し、かつ変数 `score` の値が `80` 以上である場合に「合格条件を満たしています」と出力し、そうでない場合は「条件を満たしていません」と出力するシェルスクリプトを作成してください。
なお、2つの条件は「かつ（AND）」で結合し、1つの `if` 文で記述してください。

#### ③ 繰り返し処理とコマンドの組み合わせ
**【問題】**
`1` から `5` までの数値を順番にループ処理し、各数値に対応する `dir_1` から `dir_5` までの5つのディレクトリを一括で自動作成するシェルスクリプトを作成してください。
また、作成後に `ls` コマンドを実行して、正しく作成されたかディレクトリの一覧を表示させてください。

[回答編はこちら](社員のスキルアップ教育計画_0401インフラ基礎編-シェルスクリプト-演習問題回答編.md)

---

[[ このページの先頭に戻る ]](#) [[ メニューに戻る ]](README.md)  