# このリポジトリについて

新人〜入社3年目までのエンジニアを対象とした社内研修資料を作成・保管するリポジトリです。
研修資料は **Markdown形式** で作成し、カテゴリ単位でディレクトリに整理します。

Claude Code でこのリポジトリの資料を作成・編集する際は、本ファイルのルールに従ってください。

---

## 1. ディレクトリ構成

資料は「カテゴリ」単位でディレクトリを分けます。1カテゴリ = 1ディレクトリ（ルート直下も1カテゴリ扱い）です。

| ディレクトリ | カテゴリ | ファイル名prefix |
|---|---|---|
| `/`（ルート） | 社員のスキルアップ教育計画（AI活用・インフラ・フロントエンド等の職種別カリキュラム） | `社員のスキルアップ教育計画_` |
| `/forAllEngineers/` | 全エンジニア共通知識（Git, Linux, SQL, HTTP/REST API等） | `全エンジニア共通知識_` |

新しいカテゴリを追加する場合は、新規ディレクトリを作成し、そのディレクトリ専用の prefix を決めた上で本表に追記してください。

各カテゴリディレクトリの直下には、画像を `img/`（詳細・補足画像は `img/element/`）に格納します。画像ファイル名も本文ファイルの命名規則に準じたプレフィックスを付けます（例: `S0101git_vscode-commit.png`）。

---

## 2. ファイル命名規則

```
{カテゴリprefix}_{番号}{タイトル}.md
```

- **番号**は「章-節」を表す4桁程度の数値（例: `0101`, `S0101`, `0203`）。
  - 既存ファイルと**番号が重複しないよう**、新規作成前に同カテゴリ内の既存番号を確認すること（過去に重複が発生した実績があるため要注意）。
  - 番号は将来の差し込みを考慮し、章単位・節単位で余裕を持たせる（例: `01xx`, `02xx`…）。
- タイトルは資料の内容が一目でわかる日本語名を付ける（例: `インフラ基礎編-インフラ概念`）。
- 各カテゴリには以下の特殊ファイルを置いてよい（本文資料ではないため命名・目次ルールの対象外）。
  - `{prefix}_9901cat.md`: そのカテゴリで作成予定の資料を書き溜める企画メモ（下書き・進捗管理用）
  - `{prefix}_9902ひな形.md`: 新規資料作成時にコピーして使うテンプレート

---

## 3. 資料ファイルの構成（テンプレート）

新規資料ファイルは、各カテゴリの `{prefix}_9902ひな形.md` をコピーして作成します。存在しない場合は以下の構成に従ってください。

```markdown
[[ メニューに戻る ]](README.md)

# {タイトル}

{本文}

[[ このページの先頭に戻る ]](#) [[ メニューに戻る ]](README.md)
```

- 先頭に、そのカテゴリの `README.md` に戻るリンクを置く。
- 見出しは `#`（H1）を資料タイトルに1つだけ使い、以降は `##`, `###` で構造化する。
- 各セクションの冒頭または末尾に **目的** を明記し、内容が多い場合は **キーワード** を列挙する（既存資料に倣う）。
- 図解・比較・手順は箇条書きやコードブロック（` ```plaintext `等）を積極的に使い、文章だけで説明しない。
- 想定読者は新人〜3年目までのエンジニア。専門用語には初出時に簡単な補足（例: `IAM( 認証・権限管理 )`）を添える。
- 末尾に、ページ先頭とカテゴリ`README.md`に戻るリンクを置く。

---

## 4. README.md はカテゴリの目次を兼ねる（最重要ルール）

**各カテゴリディレクトリには、そのディレクトリ内の資料の目次を兼ねる `README.md` を必ず配置・更新すること。**

- ルート直下の [README.md](README.md) が参考例です（完全ではありませんが、構成の参考にしてください）。
- サブディレクトリ（例: `forAllEngineers/`）にも、そのディレクトリ内の資料专用の `README.md` を同様に置き、目次として機能させます。現状 `forAllEngineers/README.md` は簡易的な階層メモに留まっており本文へのリンクが一部しかありませんが、今後はルート同様にフルの目次として整備・維持してください。
- 目次には最低限、以下を含めること。
  - 資料へのリンク（`👉 [資料を確認する](ファイル名.md)` 形式、または同等のリンク）
  - **目的**（この資料で何を身につけるか）
  - **キーワード**（学習内容の要点となる用語）
- カテゴリ内で章立て（大分類）がある場合は見出し（`##`）でグルーピングする。
- **資料ファイルを追加・改名・削除したら、同じPR内で該当ディレクトリの `README.md` を必ず更新すること。** 目次の更新漏れは放置しない。
- 新しいカテゴリ用ディレクトリを作成する場合は、資料を1本追加するタイミングで必ずそのディレクトリの `README.md` も同時に作成する。
- 目次専用メモ（`{prefix}_9901cat.md`）はあくまで企画・下書き用であり、`README.md` の目次の代わりにはならない。両者は役割が異なるため、企画メモを更新しても `README.md` の更新は別途必要。

---

## 5. その他の運用ルール

- 資料内容に誤りや古い情報を見つけた場合は、該当ファイルを直接修正し、必要なら `README.md` の説明文も合わせて更新する。
- 大きな構成変更（章の追加・入れ替え等）を行う場合は、着手前にどう変更するかを簡潔に示し、認識合わせをしてから進める。
- 機密情報（社内システムの実際の認証情報、個人情報等）は資料に記載しない。
