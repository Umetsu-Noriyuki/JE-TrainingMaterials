### 5. 演習問題　回答編

#### ① 四則演算と文字列の出力
**【問題】**
変数 `x` に `15`、変数 `y` に `4` を代入し、この2つの変数の「積（掛け算）」と「`x` を `y` で割った余り（剰余）」を計算して、以下のような結果をターミナルに出力するシェルスクリプトを作成してください。

**期待される実行結果：**
```text
積の計算結果: 60
余りの計算結果: 3
```

##### 💡 回答例
```bash
#!/bin/bash
x=15
y=4

echo "積の計算結果: $(( x * y ))"
echo "余りの計算結果: $(( x % y ))"
```

**【解説】**
シェルスクリプトで四則演算を行うときは、`$(( 算式 ))` という構文で囲む必要があります。掛け算には `*`、割り算の余りを求めるには `%` を使用します。画面への出力には `echo` コマンドを使用します。

---

#### ② 条件分岐（数値の比較とファイルチェック）
**【問題】**
カレントディレクトリに `test.txt` というファイルが存在し、かつ変数 `score` の値が `80` 以上である場合に「合格条件を満たしています」と出力し、そうでない場合は「条件を満たしていません」と出力するシェルスクリプトを作成してください。
なお、2つの条件は「かつ（AND）」で結合し、1つの `if` 文で記述してください。

##### 💡 回答例
```bash
#!/bin/bash
score=85

if [ -f "test.txt" -a $score -ge 80 ]; then
    echo "合格条件を満たしています。"
else
    echo "条件を満たしていません。"
fi
```

**【解説】**
- ファイルの存在（かつ通常のファイルであること）を調べるには `-f` 演算子を使用します。
- 数値の「以上」を比較するには `-ge` 演算子を使用します。
- 2つの条件を「かつ（AND）」で結ぶには `-a` を使用します。
- `if` 文の `[` と `]` の前後には**必ずスペースが必要**である点に注意してください。

---

#### ③ 繰り返し処理とコマンドの組み合わせ
**【問題】**
`1` から `5` までの数値を順番にループ処理し、各数値に対応する `dir_1` から `dir_5` までの5つのディレクトリを一括で自動作成するシェルスクリプトを作成してください。
また、作成後に `ls` コマンドを実行して、正しく作成されたかディレクトリの一覧を表示させてください。

##### 💡 回答例
```bash
#!/bin/bash
for i in {1..5}
do
    mkdir "dir_$i"
done

ls 
```

**【解説】**
- 指定した回数（範囲）だけ処理を繰り返すには `for i in {1..5}` を使用します。
- ループ内で変数 `i` の値（1〜5）をディレクトリ名に組み込む（`"dir_$i"`）ことで、動的に名前を変更して作成できます。
- 新しいディレクトリの作成には `mkdir` コマンド、一覧表示には `ls` コマンドを使用します。

---

[[ このページの先頭に戻る ]](#) [[ メニューに戻る ]](README.md)