1. AI教育資料 ... 学習にAIを役立てるためのスタートアップ用
   - [教育資料01] はここ
2. フロントエンド教育資料 ... 画面作成
3. インフラ基礎編( AWSサービス )
   - 3-1 インフラ概念 ... コスト管理の基本概念 も入れておくと実務感が増し、無駄な稼働を防げます
     - 単:Infrastructure( ITシステム基盤 )[高]
     - 単:AWS( クラウドサービス )[高]
     - ( 紹介: EC2( 仮想サーバー )[高] )
     - ( 紹介: S3( オブジェクトストレージ )[中] )
     - ( 紹介: RDS( リレーショナルDB )[中] )
     - ( 紹介: VPC( ネットワーク )[高] )
     - ( 紹介: IAM( 認証・権限管理 )[高] )
   - 3-2 管理ツール  ... IAMの利用例やベストプラクティス は非常に重要なので、必ず「権限設計の基礎」や「セキュリティ強化のポイント」を簡単に入れる
     - Management Console( 管理ツール )[高]
     - 単:Region( 地理的区分 )[高]
     - IAM( 認証・権限管理 )[高]
   - 3-3 ネットワーク・サーバー基盤
     - VPC( ネットワーク )[高]
     - EC2( 仮想サーバー )[高]
     - Fargate( コンテナ実行環境 )[高]
     - Security Group( ネットワークセキュリティ設定 )[高]
   - 3-4 サーバーレス
     - Lambda( サーバーレス実行環境 )[中]
     - API Gateway( API管理 )[中]
     - CloudWatch( 監視・ログ )[中]
   - 3-5 ストレージ・配信
     - S3( オブジェクトストレージ )[中]
     - CloudFront( CDN )[中]
   - 3-6 DB
     - RDS( リレーショナルDB )[中]
     - Athena( SQLクエリ )[中]
     - DynamoDB( NoSQL DB )[中]
   - 3-7 定時処理・通知処理
     - EventBridge( イベント連携 )[中]
     - SNS( メッセージキュー )[中]
   - 3-8 イベント処理・連携・分析
     - SQS( メッセージキュー )[中]
     - Firehose( データストリーム配信 )[低]
     - ( S3 )
     - Glue( ETL処理 )[低]
     - Redshift( データウェアハウス )[低]
   - 3-9 通信
     - 踏み台サーバ( Bastion ) [高]
     - VPN( ネットワーク接続 )[中]
     - 拠点間VPN( Site-to-Site VP ) [低]
     - Direct Connect( 専用線接続 )[中]
4. インフラ実践資料(CI/CD、Amazon Q) ... CICDやAmazon Q使用方法を想定
5. AI応用資料(LLMAI、RAG、機械学習、チューニング方法) ... AIエンジニアリング
