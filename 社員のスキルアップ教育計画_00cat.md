1. AI教育資料 ... 学習にAIを役立てるためのスタートアップ用
   - 1-1 AIリテラシー
     - ハルシネーション( 誤情報生成 )[高]
     - 機密情報保持( 情報漏洩防止 )[高]
     - プロンプト( 指示文作成 )[高]
     - 出力結果の検証( 鵜呑み防止 )[高]
     - 著作権・引用配慮( 利用ルール )[中]
     - [教育資料01] はここ
   - 1-2 AIコード作成サポート 基礎編
     - VSCode( 開発エディタ )[高]
     - AI補完( コード自動提案 )[高]
     - Chat支援( 質問応答 )[高]
     - コード生成( 雛形作成 )[高]
     - エージェント( 自律補助機能 )[中]
   - 1-3 AIコード作成サポート 実践編
     - Windsurf( 旧Codeium )[中]
     - Python( 学習用言語 )[高]
     - 自動補完( 入力効率化 )[高]
     - 実行・テスト( 動作確認 )[高]
     - エラー修正( 原因調査 )[中]
   - 1-4 AIコード作成サポート 応用編
     - デバッグ支援( 不具合解析 )[高]
     - リファクタリング( 改善提案 )[高]
     - テストコード生成( 品質向上 )[中]
     - ドキュメント生成( 説明自動化 )[中]
     - チャットAI併用( 複数AI活用 )[中]
2. インフラ基礎編( AWSサービス )
   - 2-1 インフラ概念 ... コスト管理の基本概念 も入れておくと実務感が増し、無駄な稼働を防げます
     - 単:Infrastructure( ITシステム基盤 )[高]
     - 単:AWS( クラウドサービス )[高]
     - ( 紹介: EC2( 仮想サーバー )[高] )
     - ( 紹介: S3( オブジェクトストレージ )[中] )
     - ( 紹介: RDS( リレーショナルDB )[中] )
     - ( 紹介: VPC( ネットワーク )[高] )
     - ( 紹介: IAM( 認証・権限管理 )[高] )
   - 2-2 管理ツール  ... IAMの利用例やベストプラクティス は非常に重要なので、必ず「権限設計の基礎」や「セキュリティ強化のポイント」を簡単に入れる
     - Management Console( 管理ツール )[高]
     - 単:Region( 地理的区分 )[高]
     - IAM( 認証・権限管理 )[高]
   - 2-3 ネットワーク・サーバー基盤
     - VPC( ネットワーク )[高]
     - EC2( 仮想サーバー )[高]
     - Fargate( コンテナ実行環境 )[高]
     - Security Group( ネットワークセキュリティ設定 )[高]
     - [教育資料02] はここ
   - 2-4 サーバーレス
     - Lambda( サーバーレス実行環境 )[中]
     - API Gateway( API管理 )[中]
     - CloudWatch( 監視・ログ )[中]
   - 2-5 ストレージ・配信
     - S3( オブジェクトストレージ )[中]
     - CloudFront( CDN )[中]
   - 2-6 DB
     - RDS( リレーショナルDB )[中]
     - Athena( SQLクエリ )[中]
     - DynamoDB( NoSQL DB )[中]
   - 2-7 定時処理・通知処理
     - EventBridge( イベント連携 )[中]
     - SNS( メッセージキュー )[中]
   - 2-8 イベント処理・連携・分析
     - SQS( メッセージキュー )[中]
     - Firehose( データストリーム配信 )[低]
     - ( S3 )
     - Glue( ETL処理 )[低]
     - Redshift( データウェアハウス )[低]
   - 2-9 通信
     - 踏み台サーバ( Bastion ) [高]
     - VPN( ネットワーク接続 )[中]
     - 拠点間VPN( Site-to-Site VP ) [低]
     - Direct Connect( 専用線接続 )[中]
3. インフラ実践資料( IaC )
   - 3-1 IaC基礎と実行
      - IaC（インフラをコードで管理）[高]
      - CloudFormation（AWS標準IaC）[中]
      - YAML（構成定義ファイル）[高]
      - Resources（リソース定義）[高]
      - Parameters（外部入力値）[中]
      - Outputs（出力値）[低]
      - Validate（事前構文確認）[高]
      - Deploy（環境反映）[高]
      - Update（変更適用）[高]
      - Delete Stack（環境削除）[中]
   - 3-2 サーバーレス環境をIaCで構築
      - Lambda（関数定義）[高]
      - API Gateway（API公開）[中]
      - IAM Role（実行権限）[高]
      - CloudWatch Logs（ログ出力）[高]
      - Parameter利用（環境差分切替）[中]
   - 3-3 イベント・通知環境をIaCで構築
      - EventBridge（イベント起動）[中]
      - SNS（通知配信）[中]
      - Rule（起動条件定義）[中]
      - Permission（実行許可）[中]
   - 3-4 非同期・データ連携環境をIaCで構築
      - SQS（非同期キュー）[中]
      - S3（データ保存）[中]
      - Firehose（データ配送）[低]
      - Glue（ETL処理）[低]
      - IAM Role（サービス連携権限）[高]
   - 3-5 IaC運用・設計原則
      - Tag設計（管理・コスト識別）[高]
      - IAM設計（最小権限）[高]
      - 環境分離（dev / stg / prod）[高]
      - 手作業変更禁止（IaC優先）[高]
      - Single Source of Truth（コード一元管理）[高]
      - Rollback（障害時切戻し）[中]
   - 3-6 IaC補助ツール（Amazon Q）
      - Amazon Q（IaC生成支援）[中]
      - YAML作成補助[中]
      - エラー解析支援[中]
      - テンプレート改善提案[中]
      - IAM Policy作成支援[中]
4. フロントエンド教育資料 ... 画面作成
5. コーディング実践資料( GitHub Actions )
   - 5-1 Git / GitHub 基礎運用
     - Repository( ソースコード保管庫 )[高]
     - Branch( 作業分岐 )[高]
     - Commit( 変更記録 )[高]
     - Pull Request( 変更レビュー申請 )[高]
     - Merge( 変更反映 )[高]
     - Issues( 課題管理 )[中]
   - 5-2 CI/CD 基礎概念
     - CI( 継続的インテグレーション )[高]
     - CD( 継続的デリバリー / デプロイ )[高]
     - Build( ビルド処理 )[高]
     - Test( 自動テスト )[高]
     - Deploy( 自動配置 )[高]
     - Pipeline( 自動実行工程 )[高]
   - 5-3 GitHub Actions 実践
     - GitHub Actions( GitHub標準CI/CD )[高]
     - Workflow( 自動処理定義 )[高]
     - YAML( 設定ファイル )[高]
     - Runner( 実行環境 )[中]
     - Secrets( 機密情報管理 )[高]
     - Trigger( 実行条件 )[高]
   - 5-4 AWS デプロイ連携
     - IAM Role( 権限連携 )[高]
     - OIDC( GitHub連携認証 )[高]
     - S3 Deploy( 静的サイト配置 )[高]
     - EC2 Deploy( サーバ反映 )[中]
     - ECS Deploy( コンテナ更新 )[中]
     - Lambda Deploy( 関数更新 )[中]
   - 5-5 品質保証 / 安全運用
     - Lint( コード静的検査 )[中]
     - Unit Test( 単体テスト )[高]
     - Approval( 承認フロー )[中]
     - Rollback( 切戻し )[高]
     - Artifact( 成果物管理 )[中]
     - Logs( 実行ログ確認 )[高]
   - 5-6 実務シナリオ演習
     - Push → Build → Test → Deploy( Web更新 )[高]
     - Pull Request → Review → Merge( チーム開発 )[高]
     - S3 + CloudFront 配信更新( 静的サイト )[高]
     - Lambda 自動更新( サーバーレス運用 )[中]
     - Amazon Qで調査 → 修正 → 再デプロイ( 障害対応 )[中]
     - 定期改善( CI/CD高速化・品質向上 )[中]
6. AI応用資料(LLMAI、RAG、機械学習、チューニング方法) ... AIエンジニアリング
